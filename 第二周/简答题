（1）BC
（2）type A struct {
	byte1 byte
	num1  int32
	a     struct{}
	str   string
}
所占字节为24
（3）C 当字符为 ASCII 码时则占用 1 个字节，其它字符根据需要占用 2-4 个字节，比如中文编码通常需要 3 个字节
（4）B
（5）B
（6）不是，nil 接口值既不保存值也不保存具体类型，而空接口可保存任何类型的值，这是因为nil接口中不允许调用方法，而空接口表示零个方法，因此无论什么类型都实现了零个方法，都可以去调用空接口


